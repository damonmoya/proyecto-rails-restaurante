<%= form_with(url: restaurant_mybooks_path, local: true) do |form| %>

  <div class="field">
    <%= form.label :email, 'Email' %>
    <%= form.email_field :email, :required => true%>
  </div>

  <div class="actions">
    <%= form.submit button_name %>
  </div>
<% end %>
